import {Action} from './interfaces';

let incrementAction: Action = { type: 'INCREMENT' };
let decrementAction: Action = { type: 'DECREMENT' };
let plusAction: Action = { type: 'PLUS' };

export {incrementAction, decrementAction, plusAction};
